spring:
  datasource:
    url: jdbc:h2:~/test
    username: sa
    password: sa
    driverClassName: org.h2.Driver
  h2:
    console:
      path: /h2-console
      enabled: true
      settings:
        web-allow-others: true
  jpa:
    generate-ddl: true
    hibernate:
      ddl-auto: create